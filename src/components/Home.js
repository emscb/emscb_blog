import React from "react";
import { Helmet } from "react-helmet";

const Home = ({ history }) => {
	return (
		<div>
			<Helmet>
				<title>emscb | Home</title>
			</Helmet>
		</div>
	);
};

export default Home;
